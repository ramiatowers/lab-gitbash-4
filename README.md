# LAB 4 - Práctica Git desde Terminal (macOS)

## History (comandos ejecutados)

```bash
cd lab-gitbash-4
git status
nano notas.txt
ls
cat notas.txt
git status
git add notas.txt
git status
git commit -m "Add first notes file"
nano notas.txt
cat notas.txt
git add notas.txt
git commit -m "Update notes file"
git remote -v
git push origin main
git status
history
```
